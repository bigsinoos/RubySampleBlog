<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "ایجاد توییت جدید" %>
  </div>
  <%= f.submit "پست", class: "btn btn-large btn-primary" %>
<% end %>